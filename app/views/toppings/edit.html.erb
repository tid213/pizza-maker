<h1>Edit Topping</h1>
 
<%= form_with(model: [ @pizza, @topping], local: true, data: { turbo: false }) do |form| %>
 
  <% if @topping.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@topping.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @topping.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <% if @pizza.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@pizza.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @pizza.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
  <p>
    <%= form.submit %>
  </p>
 
<% end %>

<%= link_to 'Pizza list', pizzas_path %>
<%= link_to 'Home', '/' %>